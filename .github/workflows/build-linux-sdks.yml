name: Disabled - Build - Linux (sdks)

on:
  workflow_dispatch:

jobs:
  skip:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled (replaced by build-linux.yml)."
